# Xernix testnet network identity
#
# Replaces Monero testnet constants:
#   * NETWORK_ID UUID    -> ASCII "xernix-test-net" + version byte
#   * GENESIS_NONCE      -> 21000000 (matches the 21M XRX supply cap motif)
#   * Address prefixes   -> 88 / 89 / 90 (Xernix testnet, "X" prefix)
#
# Mainnet is intentionally left untouched: Xernix launches on testnet first
# so we can iterate on consensus rules (notably consensus burn) before any
# mainnet hard fork.
#
# Pinned against MONERO_REF=v0.18.3.4. Re-base if MONERO_REF is bumped.
--- a/src/cryptonote_config.h
+++ b/src/cryptonote_config.h
@@ -261,17 +261,17 @@
 
   namespace testnet
   {
-    uint64_t const CRYPTONOTE_PUBLIC_ADDRESS_BASE58_PREFIX = 53;
-    uint64_t const CRYPTONOTE_PUBLIC_INTEGRATED_ADDRESS_BASE58_PREFIX = 54;
-    uint64_t const CRYPTONOTE_PUBLIC_SUBADDRESS_BASE58_PREFIX = 63;
+    uint64_t const CRYPTONOTE_PUBLIC_ADDRESS_BASE58_PREFIX = 88;            // Xernix testnet
+    uint64_t const CRYPTONOTE_PUBLIC_INTEGRATED_ADDRESS_BASE58_PREFIX = 89; // Xernix testnet integrated
+    uint64_t const CRYPTONOTE_PUBLIC_SUBADDRESS_BASE58_PREFIX = 90;         // Xernix testnet subaddress
     uint16_t const P2P_DEFAULT_PORT = 28080;
     uint16_t const RPC_DEFAULT_PORT = 28081;
     uint16_t const ZMQ_RPC_DEFAULT_PORT = 28082;
     boost::uuids::uuid const NETWORK_ID = { {
-        0x12 ,0x30, 0xF1, 0x71 , 0x61, 0x04 , 0x41, 0x61, 0x17, 0x31, 0x00, 0x82, 0x16, 0xA1, 0xA1, 0x11
+        0x78, 0x65, 0x72, 0x6e, 0x69, 0x78, 0x2d, 0x74, 0x65, 0x73, 0x74, 0x2d, 0x6e, 0x65, 0x74, 0x01
       } }; // Bender's daydream
     std::string const GENESIS_TX = "013c01ff0001ffffffffffff03029b2e4c0281c0b02e7c53291a94d1d0cbff8883f8024f5142ee494ffbbd08807121017767aafcde9be00dcfd098715ebcf7f410daebc582fda69d24a28e9d0bc890d1";
-    uint32_t const GENESIS_NONCE = 10001;
+    uint32_t const GENESIS_NONCE = 21000000;                                // 21M XRX cap motif
   }
 
   namespace stagenet
